# Discord Bot
A simple Discord bot that echoes user messages.

## Features
- Responds to user messages with their content.

## Installation
1. Clone the repository
2. Navigate to the project directory: `cd discord-bot`
3. Install dependencies: `npm install`

## Star the Project
```
nodemon index.js
```